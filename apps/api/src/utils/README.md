# utils/

Utilitários sem dependência de domínio.

- `app-error.ts` - classe `AppError` com status HTTP e código.
- `async-handler.ts` (a criar) - wrapper para controllers async encaminharem erros ao `errorHandler`.
- `pagination.ts` (a criar) - parsing de `page`/`pageSize` e montagem de resposta paginada.
- `phone.ts` (a criar) - normalização de telefone para E.164.
- `template-renderer.ts` (a criar) - substituição de variáveis `{{...}}` nos templates de mensagem.
